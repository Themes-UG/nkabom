<?php

function your_module_name_preprocess_block(&$variables) {
  // Check if the block is being rendered in a node context.
  if ($variables['elements']['#node']) {
    $node = $variables['elements']['#node'];
    $variables['nodeTitle'] = $node->getTitle();
  }
}



?>